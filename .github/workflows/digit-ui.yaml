name: digit-ui workflow
on:
  push:
    branches:
      - e-filling-new
    paths:
      - 'frontend/micro-ui/**'
  pull_request:
    branches:
      - e-filling-new
    paths:
      - 'frontend/micro-ui/**'
  workflow_dispatch:
jobs:
  docker_image-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          fetch-depth: 2
      - name: Setup Docker
        uses: docker/setup-buildx-action@v1

      - name: Login to Azure Container Registry
        if: github.ref == 'refs/heads/e-filling-new' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
        run: docker login -u pucarsolutionsdev -p ${{ secrets.ACR_PASSWORD }} pucarsolutionsdev.azurecr.io

      - name: Build and Push Docker image for digit-ui
        if: github.ref == 'refs/heads/e-filling-new' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
        run: |
          docker build -t digit-ui:v1.0.0-${{ github.sha }} -f web/docker/Dockerfile .
          docker tag digit-ui:v1.0.0-${{ github.sha }} pucarsolutionsdev.azurecr.io/digit-ui:v1.0.0-${{ github.sha }}
          docker push pucarsolutionsdev.azurecr.io/digit-ui:v1.0.0-${{ github.sha }}
        working-directory: frontend/micro-ui